/**
 * Climax scene stub — the Three.js heart constellation has been removed.
 * This file exports an empty init function so existing imports don't break.
 */
export function initClimaxScenes() {
  // Intentionally empty — heart particle animation removed per user request.
}
